// This page is no longer needed since we're using a modal
// You can delete this file or keep it as a fallback
import { redirect } from 'next/navigation'

export default function Login() {
  redirect('/')
}